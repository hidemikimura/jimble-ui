# 並び順を持つ一覧

管理画面の「メーカー管理」を 1 ファイルで作ってください。

- 画面見出し（題名「メーカー管理」、説明、パンくず、右に「新規登録」ボタン）
- 左のサイドバー（見出し「管理」、項目は「メーカー」と「商品」、一番下にログアウト）
- 表：つかんで並べ替えられること。並べ替えたら、画面の並びを実際に入れ替えること
- 表の列は 名前 / URLスラッグ、行の右端に「…」メニュー（編集・削除）
- 0 件のときの文言を出すこと
- データはこの中に 4 件ほど直接書いてよい（サーバとの通信は不要）
